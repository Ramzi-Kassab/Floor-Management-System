# Production Environment Configuration Example
# Copy this to .env.production and update with your actual values

# SECURITY WARNING: Generate a secure random secret key for production
# Use: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=your-secure-secret-key-here-minimum-50-characters-long

# SECURITY WARNING: Always set DEBUG to False in production
DEBUG=False

# Add your production domain(s) separated by commas
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com

# Database Configuration (PostgreSQL recommended for production)
DB_NAME=floor_management_db
DB_USER=your_db_user
DB_PASSWORD=your_secure_db_password
DB_HOST=your_db_host
DB_PORT=5432

# Email Configuration (for password reset emails)
# Uncomment and configure based on your email provider
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@example.com
# EMAIL_HOST_PASSWORD=your-email-password
# DEFAULT_FROM_EMAIL=Floor Management System <noreply@yourdomain.com>
